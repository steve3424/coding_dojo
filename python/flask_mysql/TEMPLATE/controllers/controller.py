from flask import render_template, redirect, request
from flask_app import app
from models do stuff

# Should contain the routes that are associated w/ a model
# Should call the object to do db stuff and redirect/render_template
